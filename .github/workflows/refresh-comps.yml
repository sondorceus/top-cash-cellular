name: Weekly price refresh

on:
  # Every Monday at 06:00 UTC (01:00 Austin CDT)
  schedule:
    - cron: "0 6 * * 1"
  # Manual trigger from the GitHub Actions tab
  workflow_dispatch:

jobs:
  iwm-price-table:
    name: Refresh IWM price table (all devices)
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install playwright + chromium
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          playwright install chromium --with-deps

      - name: Scrape iPhone prices (5% below IWM)
        run: python scripts/build-price-table.py iphone
        timeout-minutes: 120

      - name: Scrape Samsung prices (10% below IWM)
        run: python scripts/build-price-table.py samsung
        timeout-minutes: 60

      - name: Convert MacBook prices
        run: |
          python scripts/build-price-table.py macbook

      - name: Merge all price tables
        id: merge
        run: |
          python scripts/build-price-table.py merge | tee /tmp/merge.log
          echo "log<<EOF" >> $GITHUB_OUTPUT
          tail -50 /tmp/merge.log >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Detect changes
        id: diff
        run: |
          if git diff --quiet price-table-*.json price-table.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Summary of changes:" >> /tmp/diff-summary.txt
            git diff --stat price-table.json >> /tmp/diff-summary.txt 2>/dev/null || true
          fi

      - name: Open PR with refreshed prices
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: weekly IWM price refresh"
          title: "Weekly price refresh — IWM scrape"
          body: |
            Automated weekly refresh of device prices from ItsWorthMore.com.

            - **iPhone**: 5% below IWM (every condition × storage combo)
            - **Samsung**: 10% below IWM
            - **MacBook**: 10% below IWM (base chip/RAM config)

            Review the diff — prices that look wildly off may indicate
            a scraper issue (wrong quiz path, site change, etc).

            <details>
            <summary>Merge log</summary>

            ```
            ${{ steps.merge.outputs.log }}
            ```

            </details>
          branch: refresh-prices/weekly
          delete-branch: true

  pc-laptops:
    name: Refresh non-Apple laptop prices (IWM × 0.90)
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install playwright + chromium
        run: |
          pip install playwright
          playwright install chromium --with-deps
      - name: Scrape IWM Flawless prices for ~470 PC laptops
        # Runs single-threaded inside the runner. Originally ~40 min, but IWM
        # got slower / added models and it began timing out at 90 (stale PC
        # laptop prices for 5+ weeks). Bumped to 180; GH job cap is 6h so
        # there's headroom alongside the 60-min specs scrape below.
        run: python scripts/scrape-iwm-pc-laptop-prices.py
        timeout-minutes: 180
      - name: Apply IWM × 0.90 to page.tsx
        run: python scripts/apply-iwm-pc-laptop-prices.py
      - name: Scrape IWM additive specs (chip / RAM / storage / GPU / display)
        # Walks the embedded pricingData tree per URL — fast since no quiz
        # walk needed. ~15 min for the full pass.
        run: python scripts/scrape-iwm-laptop-specs.py
        timeout-minutes: 60
      - name: Merge spec shards + rebuild pc-laptop-specs.json
        run: |
          # The single-process scrape writes to /tmp/iwm_laptop_specs_progress.json;
          # copy it to where the build script expects.
          cp /tmp/iwm_laptop_specs_progress.json pc-laptop-iwm-adjustments.json || true
          python scripts/build-pc-laptop-specs.py
          python scripts/build-lenovo-desktop-specs.py
      - name: Run verifier
        run: python scripts/verifier.py || true
      - name: Detect changes
        id: diff
        run: |
          if git diff --quiet app/page.tsx public/comps/iwm-pc-laptop-prices.json public/comps/pc-laptop-specs.json pc-laptop-iwm-adjustments.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      - name: Open PR
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: refresh PC laptop prices + additive specs (weekly IWM)"
          title: "Weekly PC laptop refresh — prices + additive specs"
          body: |
            Automated weekly refresh of non-Apple laptop pricing in `app/page.tsx`
            and the additive spec data behind PC_LAPTOP_SPECS
            (Lenovo / HP / Dell / Alienware / ASUS / Acer / LG / Samsung Galaxy
            Book / Razer / MSI).

            **Two scrapes run in this job:**
            - Flawless base prices → `public/comps/iwm-pc-laptop-prices.json`,
              applied to `app/page.tsx` at IWM × 0.90 (Skywalker's rule).
            - Per-submodel additive deltas (chip / RAM / storage / GPU / display
              / condition / battery / charger) → `pc-laptop-iwm-adjustments.json`,
              transformed into `public/comps/pc-laptop-specs.json` for runtime
              fetch.

            Review the diff — wildly-off prices may indicate a quiz-flow
            change on IWM, or a fuzzy-match drift for brands without bridge
            slugs (Alienware / Samsung / LG).
          branch: refresh-prices/pc-laptops
          delete-branch: true

  apple-trade-in:
    name: Refresh Apple Trade-In values
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install playwright + chromium
        run: |
          pip install playwright
          playwright install chromium --with-deps
      - name: Run Apple refresh
        id: refresh
        run: |
          python scripts/refresh-apple-comps.py | tee /tmp/refresh.log
          echo "log<<EOF" >> $GITHUB_OUTPUT
          tail -200 /tmp/refresh.log >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      - name: Detect changes
        id: diff
        run: |
          if git diff --quiet public/comps/apple-trade-in.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      - name: Open PR
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: refresh Apple Trade-In comps (weekly)"
          title: "Weekly Apple Trade-In comp refresh"
          body: |
            Automated weekly refresh of `public/comps/apple-trade-in.json`.
            Values that moved >40% were kept at previous value.
          branch: refresh-comps/apple
          delete-branch: true

  samsung-trade-in:
    name: Refresh Samsung Trade-In values
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install playwright + chromium
        run: |
          pip install playwright
          playwright install chromium --with-deps
      - name: Run Samsung refresh
        id: refresh
        run: |
          python scripts/refresh-samsung-comps.py | tee /tmp/refresh.log
          echo "log<<EOF" >> $GITHUB_OUTPUT
          tail -200 /tmp/refresh.log >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      - name: Detect changes
        id: diff
        run: |
          if git diff --quiet public/comps/samsung-trade-in.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      - name: Open PR
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: refresh Samsung Trade-In comps (weekly)"
          title: "Weekly Samsung Trade-In comp refresh"
          body: |
            Automated weekly refresh of `public/comps/samsung-trade-in.json`.
          branch: refresh-comps/samsung
          delete-branch: true

  google-trade-in:
    name: Refresh Google Store Trade-In values
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install playwright + chromium
        run: |
          pip install playwright
          playwright install chromium --with-deps
      - name: Run Google refresh
        id: refresh
        run: |
          python scripts/refresh-google-comps.py | tee /tmp/refresh.log
          echo "log<<EOF" >> $GITHUB_OUTPUT
          tail -200 /tmp/refresh.log >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      - name: Detect changes
        id: diff
        run: |
          if git diff --quiet public/comps/google-trade-in.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      - name: Open PR
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore: refresh Google Store Trade-In comps (weekly)"
          title: "Weekly Google Store Trade-In comp refresh"
          body: |
            Automated weekly refresh of `public/comps/google-trade-in.json`.
          branch: refresh-comps/google
          delete-branch: true
